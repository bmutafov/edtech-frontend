import { makeStyles } from '@material-ui/styles';

export const useStyles = makeStyles({
  paper: {
    padding: 10,
    width: 240,
    boxShadow: 'none',
    border: '1px solid #cccccc',
  },
  image: {
    width: 220,
    height: 200,
    background: '#cccccc',
  },
});
